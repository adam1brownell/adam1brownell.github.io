import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://github.com/adam1brownell/adam1brownell.github.io',
  base: 'adam1brownell.github.io',
  outDir: 'dist',
});

